% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remeshing.R
\name{remeshIsotropic}
\alias{remeshIsotropic}
\title{Isotropic remeshing}
\usage{
remeshIsotropic(
  x,
  method = c("uniform", "adaptive"),
  tol = 0.001,
  edgeMin = 0.001,
  edgeMax,
  targetEdgeLen,
  nIter = 1,
  nRelaxSteps = 1,
  normals = FALSE
)
}
\arguments{
\item{x}{A \code{CGALmesh} object, i.e., the output of \code{\link[MeshUtils]{makeMesh}}.}

\item{method}{\code{character}: Either \code{"uniform"} for uniform sizing field
or \code{"adaptive"} for adapative sizing field.}

\item{tol}{Positive number for \code{method="adaptive"}:
Error tolerance. See details}

\item{edgeMin}{Positive number for \code{method="adaptive"}:
Minimum edge length. See details.}

\item{edgeMax}{Positive number for \code{method="adaptive"}:
Maximum edge length. See details.}

\item{targetEdgeLen}{Positive number for \code{method="uniform"}:
The target edge length of the remeshed mesh.}

\item{nIter}{Positive \code{integer}: Number of iterations.}

\item{nRelaxSteps}{Positive \code{integer}: Number of relaxation steps.}

\item{normals}{Boolean: Whether to return vertex normals.}
}
\value{
A \code{CGALmesh} object.
}
\description{
Isotropic remeshing.
}
\details{
See \url{https://doc.cgal.org/latest/PMP_Remeshing/} for details.
}
\examples{
library(MeshUtils)
library(rgl)

mesh         <- makeMesh(mesh=dataTruncIcosahedron, triangulate=TRUE)
mesh_rgl     <- toRGL(mesh)
mesh_rem     <- remeshIsotropic(mesh, targetEdgeLen=1)
mesh_rem_rgl <- toRGL(mesh_rem)

open3d(windowRect=50 + c(0, 0, 800, 400))
mfrow3d(1, 2)
wire3d(mesh_rgl)
next3d()
wire3d(mesh_rem_rgl)

}
\author{
Originally developed by Stephane Laurent, adapted by Daniel Wollschlaeger.
}
