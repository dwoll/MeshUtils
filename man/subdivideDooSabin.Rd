% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mesh.R
\name{subdivideDooSabin}
\alias{subdivideDooSabin}
\title{Doo-Sabin subdivision and deformation}
\usage{
subdivideDooSabin(x, nIter = 1)
}
\arguments{
\item{x}{A \code{CGALmesh} object, i.e., the output of \code{\link[MeshUtils]{makeMesh}}. The mesh must be triangle.}

\item{nIter}{\code{integer}: Number of iterations.}
}
\value{
A \code{CGALmesh} object.
}
\description{
Performs the Doo-Sabin subdivision and deformation.
}
\examples{
library(MeshUtils)
library(rgl)

mesh        <- makeMesh(mesh=dataHopfTorus)
mesh_rgl    <- toRGL(mesh)
mesh_sd     <- subdivideDooSabin(mesh, nIter=2)
mesh_sd_rgl <- toRGL(mesh_sd)

open3d(windowRect=50 + c(0, 0, 800, 400))
mfrow3d(1, 2)
view3d(0, 0, zoom=0.9)
wire3d(mesh_rgl)
next3d()
view3d(0, 0, zoom=0.9)
wire3d(mesh_sd_rgl)

}
\author{
Originally developed by Stephane Laurent, adapted by Daniel Wollschlaeger.
}
