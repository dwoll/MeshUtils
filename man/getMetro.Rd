% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hausdorff.R
\name{getMetro}
\alias{getMetro}
\title{Several distance metrics between two meshes}
\usage{
getMetro(mesh1,
         mesh2,
         symmetric = TRUE,
         p = 0.95,
         method = c("random", "grid", "mc"),
         sampleVerts = TRUE,
         sampleEdges = TRUE,
         sampleFaces = TRUE,
         gridSpacing = NULL,
         ptsOnFaces = NULL,
         ptsOnEdges = NULL,
         ptsPerDist = NULL,
         ptsPerEdge = NULL,
         ptsPerArea = NULL,
         ptsPerFace = NULL)
}
\arguments{
\item{mesh1}{A \code{CGALmesh} object, i.e., the output of \code{\link[MeshUtils]{makeMesh}}.}

\item{mesh2}{A \code{CGALmesh} object, i.e., the output of \code{\link[MeshUtils]{makeMesh}}.}

\item{symmetric}{Boolean. Whether to pool the sampled distances from
\code{mesh1} to \code{mesh2} with those from \code{mesh2} to \code{mesh1}
before taking the quantile.}

\item{p}{A number in \eqn{[0, 1]}. The quantile probability, defaults
to \code{0.95}.}

\item{method}{\code{character}. \code{"random"} for random uniform sampling,
    \code{"grid"} for grid sampling, \code{"mc"} for Monte Carlo sampling.
    See details.}

\item{sampleVerts}{Boolean. Do sample vertices?}

\item{sampleEdges}{Boolean. Do sample edges?}

\item{sampleFaces}{Boolean. Do sample faces?}

\item{gridSpacing}{\code{numeric}.}
\item{ptsOnFaces}{\code{integer}. For the random sampling method as the
  number of points to pick on the surface.
  If missing, the number of vertices is used.}

\item{ptsOnEdges}{\code{integer}. For the random sampling method as the
  number of points to pick exclusively on edges.
  If missing, the number of edges is used.}

\item{ptsPerDist}{\code{numeric}.}
\item{ptsPerEdge}{\code{numeric}.}
\item{ptsPerArea}{\code{numeric}.}
\item{ptsPerFace}{\code{numeric}.}
}
\value{
A number: the requested quantile of the sampled point-to-mesh
distances. The algorithm uses random sampling, so the result can vary.
}
\description{
Quantile Hausdorff distance (HD), average symmetric surface distance (ASSD),
and root mean squared error (RMSE) for the surface distance between two meshes.
}
\details{
See \code{\link[MeshUtils]{getHausdorffQuantile}} for details on the quantile
Hausdorff distance, \url{https://metrics-reloaded.dkfz.de/metric-library/assd} for ASSD.
Inspired by \url{http://vcglib.net/metro.html}.
For details on sampling options, see
\url{https://doc.cgal.org/latest/Polygon_mesh_processing/group__PMP__distance__grp.html}.
}
\examples{
library(MeshUtils)
getMetro(dataHeart1, dataHeart2, p=0.95, ptsOnFaces=1000L)

}
\seealso{
See \code{\link[MeshUtils]{getHausdorffQuantile}} for the quantile Hausdorff distance.
}
\author{
Daniel Wollschlaeger.
}
