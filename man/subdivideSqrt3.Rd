% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mesh.R
\name{subdivideSqrt3}
\alias{subdivideSqrt3}
\title{Sqrt3 subdivision and deformation}
\usage{
subdivideSqrt3(x, nIter = 1, triangulate = FALSE)
}
\arguments{
\item{x}{A \code{CGALmesh} object, i.e., the output of \code{\link[MeshUtils]{makeMesh}}. The mesh must be triangle.}

\item{nIter}{\code{integer}: Number of iterations.}
\item{triangulate}{Boolean: Whether to triangulate the faces. Ignored if faces
are already triangle.}
}
\value{
A \code{CGALmesh} object.
}
\description{
Performs the 'Sqrt3' subdivision and deformation.
}
\examples{
library(MeshUtils)
library(rgl)

mesh        <- makeMesh(mesh=dataHopfTorus)
mesh_rgl    <- toRGL(mesh)
mesh_sd     <- subdivideSqrt3(mesh, nIter=2)
mesh_sd_rgl <- toRGL(mesh_sd)

open3d(windowRect=50 + c(0, 0, 800, 400))
mfrow3d(1, 2)
view3d(0, 0, zoom=0.9)
wire3d(mesh_rgl)
next3d()
view3d(0, 0, zoom=0.9)
wire3d(mesh_sd_rgl)

}
\author{
Originally developed by Stephane Laurent, adapted by Daniel Wollschlaeger.
}
