% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subdivide.R
\name{triangulateMesh}
\alias{triangulateMesh}
\title{Triangulate mesh}
\usage{
triangulateMesh(x, normals = FALSE)
}
\arguments{
\item{x}{A \code{CGALmesh} object, i.e., the output of \code{\link[MeshUtils]{makeMesh}}.
The mesh must be triangle or be able to be made triangle.}

\item{normals}{Boolean: Whether to return vertex normals.}
}
\value{
A \code{CGALmesh} object.
}
\description{
Triangulate mesh.
}
\examples{
library(MeshUtils)
library(rgl)

f_mesh   <- system.file("extdata", "corner.off", package="MeshUtils")
mesh_vf  <- readMeshFile(f_mesh)
mesh     <- makeMesh(mesh_vf[["vertices"]], mesh_vf[["faces"]])
mesh_rgl <- toRGL(mesh)
isTriangle(mesh)

mesh_tri     <- triangulateMesh(mesh)
mesh_tri_rgl <- toRGL(mesh_tri)
open3d(windowRect=50 + c(0, 0, 800, 400))
mfrow3d(1, 2)
wire3d(mesh_rgl)
next3d()
wire3d(mesh_tri_rgl)

}
\author{
Originally developed by Stephane Laurent, adapted by Daniel Wollschlaeger.
}
