% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alpha_wrap.R
\name{alphaWrap}
\alias{alphaWrap}
\title{3D alpha wrapping}
\usage{
alphaWrap(x, alphaRel, offsetRel, out = c("CGALmesh", "rgl"))
}
\arguments{
\item{x}{Numeric matrix which stores the points, one point per row.}

\item{alphaRel}{Relative alpha parameter. The actual alpha parameter (see
details) is defined as the length of the diagonal of the bounding box of
the point cloud divided by the relative alpha parameter.}

\item{offsetRel}{Relative offset. The actual offset parameter (see details)
is defined as the length of the diagonal of the bounding box of the
point cloud divided by the relative offset parameter.}

\item{out}{Character to indicate output mesh format.}
}
\value{
A \code{CGALmesh} object or a \code{\link[rgl]{mesh3d}} object from package
\strong{rgl}.
}
\description{
Reconstruction of a surface from a cloud of 3D points by
alpha wrapping.
}
\details{
See \href{https://doc.cgal.org/latest/Alpha_wrap_3/index.html}{3D Alpha Wrapping}
for details. The smallest alpha parameter, the smallest triangles in the
output mesh. The offset is the distance from the input points to the output
mesh.
}
\examples{
library(MeshUtils)
library(rgl)
# reconstruct the mesh from the points
mesh_alwrap_rgl <- alphaWrap(dataHeart1[["vertices"]],
                             alphaRel =70,
                             offsetRel=3000,
                             out      ="rgl")

# plot
shade3d(mesh_alwrap_rgl, color="forestgreen")

}
\seealso{
\code{\link[MeshUtils]{reconstructAFS}},
\code{\link[MeshUtils]{reconstructSSS}},
\code{\link[MeshUtils]{reconstructPoisson}}
}
\author{
Originally developed by Stephane Laurent, adapted by Daniel Wollschlaeger.
}
